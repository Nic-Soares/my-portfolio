import styles from "./ProjectDetailView.module.css";
import Container from "@/components/common/container/Container";

type Props = {};

const ProjectDetailView = (props: Props) => {
  return (
    <header>
      <Container />
    </header>
  );
};

export default ProjectDetailView;
